.container
  .row
    .col-xs-12
      %a{:'ui-sref' => 'admin-patients-new'}
        dodaj pacjenta
      %table.table.table-bordered
        %thead
          %tr
            %th
              Pesel
            %th
              Imię
            %th
              Nazwisko
            %th
              Akcje
        %tbody
          %tr{:'ng-repeat' => 'patient in patients'}
            %td
              %a{:'ui-sref' => 'admin-patients-show({patientId: patient.id})'}
                {{patient.id}}
            %td
              %a{:'ui-sref' => 'admin-patients-show({patientId: patient.id})'}
                {{patient.name}}
            %td
              %a{:'ui-sref' => 'admin-patients-show({patientId: patient.id})'}
                {{patient.surname}}
            %td
              %a.btn.btn-danger{:'ng-click' => 'remove(patient.id)'}
                Usuń
              %a.btn.btn-primary{:'ui-sref' => 'admin-patients-edit({patientId: patient.id})'}
                Edytuj
              %a.btn.btn-primary{:'ui-sref' => 'admin-patients-visits({patientId: patient.id})'}
                Wizyty
